---
import ShowcaseView from '@/components/pages/views/ShowcaseView.astro';
import { defaultLocale } from '@/i18n';

// The Showcase page for the default locale. Additional locales are served by
// `src/pages/[locale]/showcase.astro`, which renders the same view.
---

<ShowcaseView locale={defaultLocale} />
