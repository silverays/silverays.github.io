---
title: "Rockplate"
description: A lightweight JavaScript template engine for Node.js and the browser, focused on simplicity and performance.
locale: en
tags: ["JavaScript", "Node.js", "Open Source"]
featured: false
order: 2
year: 2018
role: "Creator"
url: "https://github.com/rockplate/rockplate"
repo: "https://github.com/rockplate/rockplate"
services: ["Open Source", "Tool Development"]
meta: ["Zero dependencies", "Under 5KB"]
---

A minimal, fast template engine with an intuitive syntax that worked in both Node.js and browser environments without dependencies.

## Architecture

Rockplate compiled templates into JavaScript functions at runtime. The parser tokenised the template string, the compiler generated a function body, and the runtime executed it with the provided data context.

Key technical decisions:

- **Compile-to-function** — templates compiled once and cached
- **Simple syntax** — readable by non-developers
- **Sandboxed execution** — compiled functions without `eval`
